# C++17 corpus expected output

Each expected file is paired with a manifest case. Negative cases use the
literal marker `compile_error`; UB probes use `undefined_behavior` and are
reported, not scored as correctness tests.

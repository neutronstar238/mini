  </main>

  <!-- 页脚版权（部署上线时按需补充备案信息，仓库内不保留个人备案号） -->
  <footer class="cpc-footer">
    <div class="flex-between" style="justify-content:center;gap:24px;flex-wrap:wrap">
      <span>Mini-OJ · 本地预检 + 可信边缘评测</span>
    </div>
  </footer>

  <div id="toast-root"></div>
  <script src="/js/common.js"></script>
  <script>
    (function () {
      var btn = document.getElementById('logout-btn');
      if (btn) btn.addEventListener('click', async function () {
        try { await api('/api/contest/auth/logout', { method: 'POST' }); } catch (_) {}
        localStorage.removeItem('token');
        location.href = '/admin/login';
      });
    })();
  </script>
  <% if (typeof pageScript !== 'undefined' && pageScript) { %>
  <script src="<%= pageScript %>"></script>
  <% } %>
</body>
</html>
